package;

import found.Found;

class Main {
	public static function main(){
		//To see the whole FoundConfig go have a look at of Found class in Foundry2d
		Found.setup({app:Project, title:'MyProjectName'});
	}
}